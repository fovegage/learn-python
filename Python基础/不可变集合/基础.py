# -*- coding: utf-8 -*-
# @Time    : 2018/12/29 17:04
# @Author  : fovegage
# @Email   : fovegage@gmail.com
# @File    : 基础.py
# @Software: PyCharm

# set无序排序且不重复，是可变的，有add（），remove（）等字典的key，也可以作为其它集合的元素。缺点是一旦创建便不能更改，没有add，remove方法方法。
# 既然是可变的，所以它不存在哈希值。基本功能包括关系测试和消除重复元素. 集合对象还支持union(联合), intersection(交集), difference(差集)和sysmmetric difference(对称差集)等数学运算.
# # # sets 支持 x in set, len(set),和 for x in set。作为一个无序的集合，sets不记录元素位置或者插入点。因此，sets不支持 indexing, 或其它类序列的操作。
# # # frozenset是冻结的集合，它是不可变的，存在哈希值，好处是它可以作为

# 可以用来做字典的key值